import 'package:flutter/material.dart';

class Questionario extends StatelessWidget {
  final List<Map<String, Object>> _perguntas;


  Questionario(this._perguntas);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
